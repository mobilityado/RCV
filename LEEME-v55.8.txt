REPORT.IA RGI v55.8

Corrección de publicación:
- Los bloques POST solo se guardan; el último ya no intenta construir el snapshot dentro del iframe.
- La finalización se solicita por GET/JSONP, mecanismo que sí aparece en Ejecuciones de Apps Script.
- El servidor devuelve directamente snapshotId.
- Se elimina el ciclo de 60 consultas que podía tardar decenas de minutos.
- El finalizador evita parsear y volver a serializar todo el JSON; escribe en lotes de 30,000 caracteres.

Instalación:
1. Sustituye Código.gs por codigo-google-RGI-NUBE-v55_8.txt.
2. Guarda y crea NUEVA VERSIÓN de la MISMA implementación web. No cambies la URL/ID.
3. Sube los archivos del portal a GitHub.
4. Ctrl+Shift+R y confirma v55.8.
5. Procesa Gastos y Publicar en nube una sola vez.
